---
title: Inner join
headline: Inner join
def: A join that only keeps rows where the keys from the input tables matched.
redirect_from:
  - /glossary/inner_join
---

## What is an inner join?

An inner join is a join that only keeps rows where the keys from the input tables matched.
