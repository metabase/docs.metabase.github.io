---
title: "Transactional database"
headline: "Transactional database"
def: "A database optimized for a large number of small operations rather than a few large transactions. OLTP usually relies on transactional databases."
related-terms:
  - term: OLTP
    slug: oltp
  - term: Analytical database
    slug: analytical_database
redirect_from:
  - /glossary/transactional_database
---

## What is a transactional database?

A transactional database is a database optimized for a large number of small operations rather than a few large transactions. OLTP usually relies on transactional databases.
