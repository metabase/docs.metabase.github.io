---
title: Full outer join
headline: Full outer join
def: A join that keeps every row from both tables regardless of whether keys matched.
related-terms:
  - term: Join
    slug: join
redirect_from:
  - /glossary/full_outer_join
---

## What is a full outer join?

A full outer join is a join that keeps every row from both tables regardless of whether keys matched. Columns in one table are filled with `NULL` where there wasn't a match in the other table.
