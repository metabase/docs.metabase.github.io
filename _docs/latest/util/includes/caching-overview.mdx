import CachingPlanNote from "./caching-plan-note.mdx";

{/* Partial: imports another MDX partial. */}

Caching stores the results of **{props.name ?? "a query"}** so Metabase can serve them without re-running the query.

<CachingPlanNote feature={props.feature ?? "Caching"} />
