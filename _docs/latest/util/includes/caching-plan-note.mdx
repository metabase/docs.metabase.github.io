import LiquidInclude from "@/components/LiquidInclude.astro";

{/* Partial: wraps the Liquid plans blockquote. Pass `feature` through. */}

<LiquidInclude src="plans-blockquote.html" feature={props.feature} />
